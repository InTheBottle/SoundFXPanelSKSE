#pragma once

#include "RE/Skyrim.h"
#include "SKSE/SKSE.h"

// Standard library headers used widely across this plugin
#include <algorithm>
#include <filesystem>
#include <format>
#include <ranges>
#include <string>
#include <string_view>
#include <unordered_map>
#include <vector>

// Logging (default spdlog logger configured by SetupLog)
#include <spdlog/spdlog.h>

using namespace std::literals;
